<h3>Following (<%= @user.following.count %>)</h3>
<p>DJ's <%= @user.name %> is currently following!</p>
<% if @following.any? %>
	<% cache(cache_key_for_following (@user)) do %>
	<ul class="users follow">
	  <%= render @following %>
	</ul>
	<%= will_paginate @following%>
	<% end %>
<% end %>