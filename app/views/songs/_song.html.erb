<li id="songs-<%= song.id %>">
  <span class="title"><%= song.title %></span>
  <span class="artist"><%= song.artist %></span>
  <span class="album"><%= song.album %></span>
  <button class='add-button'><span>Play</span></button>
</li>